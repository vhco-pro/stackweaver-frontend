# Generated from vhco-pro/.github workflow-templates/codeql-js.yml

name: CodeQL
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "33 4 * * 2"


jobs:
  analyze:
    if: ${{ github.event.repository.visibility == 'public' }}
    uses: vhco-pro/.github/.github/workflows/reusable-codeql-js.yml@main
    permissions:
      security-events: write
      contents: read
      actions: read
